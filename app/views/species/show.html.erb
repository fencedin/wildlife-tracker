<h1><%= @species.name %></h1>

<ul>
  <% @species.sightings.each do |sighting| %>
    <li>
      <p><%= sighting.date %>, <%= sighting.location %>, Region: <%= sighting.region_id %>
      <a href="/sightings/<%= sighting.id %>/edit">edit</a>
      <a href="/sightings/<%= sighting.id %>"
        data-confirm="Are you sure?"
        data-method="delete"
        rel="nofollow>">Delete</a></p>
    </li>
  <% end %>
</ul>

<p><a href="/">Go back to Species List</a></p>
